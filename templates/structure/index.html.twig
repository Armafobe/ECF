{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block content %}
    <div class="flex w-full items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
        <ul role="list" class="grid w-1/3 grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
            <li class="col-span-3 divide-y divide-gray-200 rounded-lg bg-white shadow">
                <div class="flex items-center justify-between space-x-6 p-6">
                    <div class="flex-1 truncate">
                        <div class="text-center space-x-3">
                            <h3 class="truncate text-sm font-medium text-gray-900">{{ structure.address }}</h3>
                            {#                            {% if active %}#}
                            {#                            <span class="inline-block flex-shrink-0 rounded-full bg-green-100 px-2 py-0.5 text-xs font-medium text-green-800">Actif</span>#}
                            {#                            {% else %}#}
                            {#                            <span class="inline-block flex-shrink-0 rounded-full bg-red-400 px-2 py-0.5 text-xs font-medium text-green-800">Inactif</span>#}
                            {#                            {% endif %}#}
                            <p class="mt-1 truncate text-sm text-gray-500">Regional Paradigm Technician</p>
                        </div>
                    </div>
                </div>
            </li>
        </ul>
    </div>
{% endblock %}
